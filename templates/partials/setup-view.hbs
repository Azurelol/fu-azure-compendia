<article class="sk-box">
    <h4>{{data.label}}</h4>
    <p>{{data.description}}</p>
    <ul class="sk-check {{#if data.extended}}sk-two{{/if}}">
        {{#each data.choices as |choice index|}}
            <li>
                {{choice.text}}
            </li>
        {{/each}}
    </ul>
</article>
